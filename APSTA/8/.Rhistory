setwd("W:/Projects/ITU/APSTA/8")
firstchi
library(UsingR)
library(UsingR)
firstchi
firstchi
mean(firstchi)
sv = var(firstchi)
prcnt = quantile(firstchi, .30)
prcnt = quantile(firstchi, .30)
prcnt
prcnt = quantile(firstchi, .30)[1]
prcnt
prcnt = quantile(firstchi, .30)
mn = mean(firstchi)
sv = var(firstchi)
prcnt = quantile(firstchi, .30)
prcnt
prcnt
?quantile
md = median(firstchi)
md <- mad(firstchi)
mn <- mean(firstchi)
sv <- var(firstchi)
prcnt <- quantile(firstchi, .30)
mdn <- median(firstchi)
md <- mad(firstchi)
sprintf("Mean: %s\nSample variance: %s\n30th percentile:%s\nMedian: %s\nMAD: %s", mn, sv, prcnt, mdn, md)
cat(sprintf("Mean: %s\nSample variance: %s\n30th percentile:%s\nMedian: %s\nMAD: %s", mn, sv, prcnt, mdn, md))
mn <- mean(firstchi)
sv <- var(firstchi)
prcnt <- quantile(firstchi, .30)
mdn <- median(firstchi)
md <- mad(firstchi)
cat(sprintf("Mean: %s\nSample variance: %s\n30th percentile:%s\nMedian: %s\nMAD: %s", mn, sv, prcnt, mdn, md))
cat(sprintf("Mean: %s\nSample variance: %s\n30th percentile:%s\nMedian: %s\nMAD: %s", mn, sv, prcnt, mdn, md))
```
diamond
?density
kernel = density(diamond$carat, bw="SJ")
plot(kernel)
est <- ecdf(diamond$carat)
plot(est)
kernel <- density(diamond$carat, bw="SJ")
plot(kernel)
est <- ecdf(diamond$carat)
plot(est)
plot(diamond$price, diakond$size)
plot(diamond$price, diamond$size)
